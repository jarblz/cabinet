<nav class="navbar navbar-default home-nav" id="navbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">UnBoxt</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= current_user.try(:name) %> <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <%= render 'layouts/profile_links' %>
        </ul>
      </li>
    </ul>
  </div><!-- /.container-fluid -->
</nav>
